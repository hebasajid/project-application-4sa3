Make sure you are in the projects directory. 
In the terminal, type npm install to
install all dependencies. 

be in the root folder of the project and run "node app.js"
to see the application run.

.env file includes the secret key and mongoURI which are not pushed
to github to maintain privacy as it is a public repository.